{
  "name": "factory-clerk-mcp",
  "version": "1.0.0",
  "description": "MCP server for Clerk — user lookup, session inspection, auth config review (read-only)",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
